1. Install next with tailwindcss

```shell
npx create-next-app --example with-tailwindcss medium
```

2. Install mnjs

```shell
npm i mnjs
```

3. Install sanity

```shell
npm install -g @sanity/cli && sanity init
```
